#include"main.h"

/**
 * main - function that checks for a digit (0 through 9)
 * @c : character to be checked
 * Returns : 1 if c is a digit
 * Returns : 0 otherwise
 */

int _isdigit(int c)
{
	return(c > 0 && c < 10);
	return(c = 0);
}
